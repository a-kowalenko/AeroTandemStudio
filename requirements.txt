# GUI Framework
# 0.4.4 ships a 32-bit DLL in win-x64 (breaks 64-bit Windows builds)
tkinterdnd2==0.4.3
tkcalendar

# Video/Medien Processing
moviepy>=1.0.3
python-vlc
opencv-python

# QR/Barcode Scanner
pyzbar

# Logging
proglog

# Bildverarbeitung
Pillow

# System Utilities (Platform specific)
pywin32; sys_platform == 'win32'
pyudev; sys_platform == 'linux'
psutil; sys_platform != 'win32'

# HTTP/Networking
requests
urllib3

# Version Management
packaging

# Testing (Development)
pytest
pytest-mock

# SMB Server (Development/Testing)
# impacket - hat Build-Probleme auf Windows, verwenden Sie stattdessen:
# - tests/simple_smb_server.py (Windows net share)
# - tests/smb_mock.py (Mock für Unit-Tests)
